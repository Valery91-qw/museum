const animationClassToLeft = 'to-left';
const animationClassFromLeft = 'from-left';
const animationClassToRight = 'to-right';
const animationClassFromRight = 'from-right';
const nextClass = 'next';
const activeClass = 'active';
const startItem = 0;

export default {
  animationClassFromLeft,
  animationClassToLeft,
  animationClassFromRight,
  animationClassToRight,
  startItem,
  nextClass,
  activeClass,
}